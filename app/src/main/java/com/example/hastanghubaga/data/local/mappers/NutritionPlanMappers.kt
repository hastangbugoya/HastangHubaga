package com.example.hastanghubaga.data.local.mappers

/**
 * Legacy compatibility file retained only to avoid broken file references during
 * the nutrition architecture migration.
 *
 * All normalized nutrition mapper functions now live in:
 * - NutritionGoalsMappers.kt
 *
 * This file intentionally contains no mapper functions to avoid duplicate
 * top-level extension declarations in the same package.
 *
 * Future cleanup:
 * - Delete this file once the migration is complete.
 */