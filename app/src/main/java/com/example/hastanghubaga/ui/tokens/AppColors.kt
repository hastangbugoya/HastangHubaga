package com.example.hastanghubaga.ui.tokens

import androidx.compose.ui.graphics.Color

/**
 * App color tokens (your design system source of truth).
 *
 * Keep these as plain tokens (no MaterialTheme dependency),
 * so you can reuse them anywhere (previews, components, theme mapping).
 *  AppColors > Theme
 */
// AppColors > Theme > UiColors > Composable
object AppColors {
    // Light
    val PrimaryLight = Color(0xFF6750A4)
    val SecondaryLight = Color(0xFF625B71)
    val ErrorLight = Color(0xFFB3261E)

    // Dark
    val PrimaryDark = Color(0xFFD0BCFF)
    val SecondaryDark = Color(0xFFCCC2DC)
    val ErrorDark = Color(0xFFF2B8B5)
}

